import { call, put, takeEvery, select, fork } from 'redux-saga/effects'

// const checkIfLoggedIn = function* (action) {
//   yield put({ type: 'CHECK_IF_LOGGED_IN' })
// }

export default [ function* () {
  // yield fork(checkIfLoggedIn)
  // yield* takeEvery('SAY_HI_ASYNC_START', thinkHowToSayHi)
} ]
